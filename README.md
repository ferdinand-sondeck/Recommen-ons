# Recommen-ons

## API Routes

### `GET /api/services`
Renvoie la liste des services disponibles.

### `GET /api/status`
Renvoie l’état de l’application.

## Lancer le projet

```bash
npm install
node app.js